(()=>{const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];const cfg=window.LDU_CONFIG||{};q('#year').textContent=new Date().getFullYear();addEventListener('scroll',()=>{const h=document.documentElement;const p=h.scrollTop/(h.scrollHeight-h.clientHeight);q('#progress').style.width=(p*100)+'%'});const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.12});qa('.reveal').forEach(el=>io.observe(el));const menu=q('#menu'),mobile=q('#mobileNav');menu.addEventListener('click',()=>{mobile.hidden=!mobile.hidden;menu.setAttribute('aria-expanded',String(!mobile.hidden))});qa('#mobileNav a').forEach(a=>a.addEventListener('click',()=>mobile.hidden=true));
const lang=q('#lang');lang.addEventListener('click',()=>{const ar=document.documentElement.lang!=='ar';document.documentElement.lang=ar?'ar':'en';document.documentElement.dir=ar?'rtl':'ltr';lang.textContent=ar?'AR / EN':'EN / AR';document.body.classList.toggle('rtl',ar)});
const call=q('#callBtn');if(call){const n=cfg.PHONE_E164||'+966540211883';call.setAttribute('href','tel:'+n);call.addEventListener('click',()=>{call.setAttribute('href','tel:'+n)})};
const cookie=q('#cookie');if(!localStorage.getItem('ldu-consent'))cookie.hidden=false;q('#essential').addEventListener('click',()=>{localStorage.setItem('ldu-consent','denied');cookie.hidden=true});q('#allow').addEventListener('click',()=>{localStorage.setItem('ldu-consent','granted');cookie.hidden=true;dispatchEvent(new Event('ldu:consent'))});
const form=q('#leadForm'),status=q('#formStatus');form.addEventListener('submit',async e=>{e.preventDefault();status.textContent='';if(!form.checkValidity()){form.reportValidity();return}const fd=new FormData(form);if(fd.get('website'))return;const endpoint=cfg.CONTACT_ENDPOINT;if(!endpoint){status.textContent='Lead capture endpoint is not configured yet. Please use the direct line.';return}const payload=Object.fromEntries(fd.entries());try{const r=await fetch(endpoint,{method:cfg.CONTACT_ENDPOINT_METHOD||'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!r.ok)throw new Error('bad response');status.textContent='Thank you. Your enquiry was submitted.';form.reset();dispatchEvent(new Event('ldu:lead'))}catch(_){status.textContent='Submission failed. Please use the direct line.'}});
})();
